package cookbook;

import cookbook.frontend.UIHandler;
import javafx.application.Application;

// Launches the Application
public class App {
    public static void main(String[] args) {
        Application.launch(UIHandler.class, args);
    }
}